#include <stdio.h>
#define MAX 100
int q[MAX], front=-1, rear=-1;
void enqueue(int x) {
    if (rear==MAX-1) printf("Overflow\n");
    else {
        if (front==-1) front=0;
        q[++rear]=x;
    }
}
int dequeue() {
    if (front==-1 || front>rear) { printf("Underflow\n"); return -1; }
    return q[front++];
}
void display() {
    int i;
    if (front==-1 || front>rear) { printf("Empty\n"); return; }
    for (i=front;i<=rear;i++) printf("%d ",q[i]);
    printf("\n");
}
int main() {
    int ch, x;
    do {
        printf("1.Enqueue 2.Dequeue 3.Display 4.Exit: ");
        scanf("%d",&ch);
        if (ch==1){ printf("Value: "); scanf("%d",&x); enqueue(x); }
        else if (ch==2){ x=dequeue(); if (x!=-1) printf("Dequeued %d\n",x); }
        else if (ch==3) display();
    } while (ch!=4);
    return 0;
}
